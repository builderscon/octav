language: go
go:
  - 1.6
services:
  - mysql
env:
  -
  - DIR=$TRAVIS_BUILD_DIR/octav
install:
  - echo "skipping install step"
script:
  - cd $DIR && ./travis.sh
after_failure:
  - echo "Checking out $DIR/debug.out"
  - test -e "$DIR/debug.out" && cat "$DIR/debug.out"
